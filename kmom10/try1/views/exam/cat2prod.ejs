<%- include("../partials/header", {title: title, path: path}); %>

<h1>Produkter i kategori: <%= cat.kategorinamn %> </h1>

<table>
    <thead>
        <tr>
            <th>Id</th>
            <th>Namn</th>
            <th>Bildlänk</th>
        </tr>
    </thead>
    <tbody>
    <% for (const row of prods) { %>
        <tr>
            <td class="right"><%= row.id %></td>
            <td><%= row.namn %></td>
            <td class="right"><%= row.lank %></td>
        </tr>
    <% }; %>
    </tbody>
</table>

<%- include("../partials/footer"); %>
